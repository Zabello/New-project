import { NewFeaturePipe } from './new-feature.pipe';

describe('NewFeaturePipe', () => {
  it('create an instance', () => {
    const pipe = new NewFeaturePipe();
    expect(pipe).toBeTruthy();
  });
});
